@model HomeModel

<div id="lastPost" class="padding-bottom-medium backround-color-grey">
    <h2 class="text-center font-weight-500 margin-top-small margin-bottom-small">Ultimi post</h2>
    <div class="carousel-wrapper">
        <div class="owl-carousel owl-theme">
            @foreach (var post in Model.PostDisplays)
            {   
                <div id="@post.id" class="item carousel__photo text-center" >
                    <section class="card-head border-radius-small box-shadow" style="background-image: url('@post.coverImage')"></section>
                    <section class="card-body border-radius-small padding-xsmall box-shadow margin-bottom-medium">
                        <h3 class="font-weight-600">@post.title</h3>
                        <p>@Html.Raw(post.testo)</p>
                    </section>
                    <a class="text-center background-color-blue color-white border-radius-small text-uppercase btn readmore" href="@post.slug"> Leggi di più </a>
                </div>
            }
        </div>
    </div>
</div>
